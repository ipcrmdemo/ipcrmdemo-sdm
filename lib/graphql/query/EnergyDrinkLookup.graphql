query EnergyDrinkLookup ($sha: String!){
    Push {
        commits (sha: $sha) @required{
            sha
            repo {
                id
                owner
                name
                org {
                    scmProvider {
                        providerId
                    }
                }
            }
        }
        branch
    }
}
